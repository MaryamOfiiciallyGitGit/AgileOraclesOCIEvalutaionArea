spring.application.name=leave-portal-app
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

oci.region=${OCI_REGION:us-ashburn-1}
oci.namespace=${OCI_NAMESPACE:idqag2xakgns}
oci.bucket-name=${OCI_BUCKET_NAME:leave-bucket-pair-04}
oci.config-file=${OCI_CONFIG_FILE:C:/Users/mahmo/Desktop/.oci/config}
oci.profile=${OCI_PROFILE:DEFAULT}

server.port=${SERVER_PORT:8080}

gemini.api-key=${GEMINI_API_KEY}
spring.datasource.url=jdbc:oracle:thin:@//${DB_HOST:localhost}:${DB_PORT:1527}/FREEPDB1
spring.datasource.username=${DB_USERNAME:eta_maryam}
spring.datasource.password=${DB_PASSWORD:firstpeerpass}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

